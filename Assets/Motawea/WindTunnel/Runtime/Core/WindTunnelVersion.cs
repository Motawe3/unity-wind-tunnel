namespace Motawea.WindTunnel
{
    /// <summary>
    /// Release version, stamped into exported sessions so a comparison can tell that
    /// two results came from different builds of the solver.
    /// Keep in sync with the release tag.
    /// </summary>
    public static class WindTunnelVersion
    {
        public const string Value = "1.0.0";
    }
}
